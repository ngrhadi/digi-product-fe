/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  assetPrefix: '',
  publicRuntimeConfig: {
    basePath: '',
  },
  basePath: '',
  swcMinify: false,
  output: 'standalone',
};

export default nextConfig;
